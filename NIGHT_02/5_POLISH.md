# NIGHT_02 tier-1 doc 5 — cross-cutting polish + final perceptual batch

Parent: `TIER0_MASTER.md` item 0-5. Harness (doc 1) required.

- **5-1 Font-object audit.** Sweep every frame touched in docs 2–4: is each
  text element using the correct reference FONT object and size? CSV of
  frame → expected font → actual font → delta. Fix mismatches (additive).
- **5-2 Color-constant audit.** Class colors, item-quality colors, reaction/
  faction colors, power-type bar colors, quest-difficulty colors vs the
  reference constants (hex compare). CSV verdict; fix mismatches.
- **5-3 Strata / layer audit.** Frame strata and draw-layer order vs
  reference across all touched frames; catch panels drawing over/under the
  wrong things. CSV verdict.
- **5-4 Backdrop / nine-slice audit.** Every backdropped frame's edge/corner
  texture set and insets vs reference.
- **5-5 Final full-UI contact batch.** One labeled, run-dated side-by-side
  contact sheet set covering every panel from docs 2–4 in a representative
  state, for Nico's SINGLE perceptual sign-off pass. This is the run's
  headline deliverable to Nico — one queue entry, all sheets referenced.
- **5-6 Hygiene + end packet.** Manifests recompute (immutable-artifact
  scope only, per Q21 pre-ruling); no stray raw art dumps; four+interface
  gates green; end-of-run packet per Tier 0.
