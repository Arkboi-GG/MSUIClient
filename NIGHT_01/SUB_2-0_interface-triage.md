# NIGHT_01 sub-doc 2-0 — whole-item interface triage

Parent: `2_INTERFACES.md`. All Tier-0 rules apply.

## Scope fence

This pass inventories whether each interface can enter its required interface
loop without a partial implementation. It changes no opcode list, UI, server,
DB, baseline, or gameplay behavior.

## Acceptance

An item proceeds only if its complete wire family, client state, reversible GM
fixture, scripted runner actions, and mechanical acceptance instrument already
exist or can be completed as one bounded root cause. Otherwise the parent doc's
time-pressure rule shelves the whole item.

## Evidence

`live-runs/N2-interface-triage-20260731-234000.md` and its manifest.

