# NIGHT_01 subdocument 1-2 — combat matrix completion

Parent: `1_COMBAT_ROOTCAUSE.md` item 1-2. Tier-0 and SPEC-21 remain authoritative.

## 1-2-1 — exact re-anchor runner primitive

Add runner-only `anchor spawn:n`, reusing the authoritative `SpawnObserved`
identity and the existing `.go xyz` path. It exists only to return the player
to the same descriptor position after the range discriminator, allowing the
unchanged Z0 gate to pass at distance zero. No combat send bypass.

Acceptance: spawned GUID is preserved, server teleport applies, subsequent
`AttackPreconditionGatePass` names the same GUID at distance zero. Four gates.

## 1-2-2 — V-A through V-D

Fresh target per cell, exact cleanup, socket + combat traces. V-A records the
expected GM-on Z0 refusal; V-B is the GM-off valid send; V-C records a nonzero-
distance refusal then exact re-anchor and valid send; V-D sends at zero distance
while turned away, then turns in. Player-GUID-only response accounting.

## 1-2-3 — CB4/CB5/CB6/CB7 pack

Fresh descriptors, GM off, exact pre-send gate on every constructed attack.
CB4 cancel/re-arm; CB5 two-target switch; CB6 descriptor-confirmed death; CB7
movement/intent overlap. Run combat-audit and report cadence versus weapon speed
as PASS/FAIL/NO_DATA honestly. Exclude foreign attacker rows.

## 1-2-4 — P4 finding packet

Freeze the completed table, add any fix decision to the rulings queue, append
item 1-2 status to progress, four gates, manifest, commit, anti-lost loop.
