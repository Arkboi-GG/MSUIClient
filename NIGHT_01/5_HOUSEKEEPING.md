# NIGHT_01 tier-1 doc 5 — housekeeping backlog

Parent: `TIER0_MASTER.md` item 0-5. All Tier-0 rules apply. These are the
queued non-perceptual items from the pilot handoff and report tail.

- **5-1 Panes/keybinds slice P.** Per `GAMEPLAY_FOUNDATION_PLAN_2.md`,
  slice P as specified there; instrument-first, additive.
- **5-2 Panes/keybinds slice K.** Same source, slice K.
- **5-3 Initial-IntentOff audit hygiene.** The queued determination:
  reproduce, classify (real defect vs audit artifact), evidence, queue
  entry if a law/list change is implied.
- **5-4 Benilla golden traces (I14 stage B).** Capture/commit the golden
  traces per the I14 plan; comparison against MSUI traces is mechanical
  and yours, any behavior-change conclusion goes to the queue.
- **5-5 Backpedal animation evidence.** Collect the queued evidence
  (trace + contact sheet) so Nico's morning ruling has artifacts; the
  ruling itself stays his.
- **5-6 Deferred-list triage.** Read `portrait-known-blank.txt` and
  `variant-items-known-issues.txt`; for each entry, ONE line in the
  queue: still-reproduces / no-longer-reproduces / needs-Nico, with a
  fresh evidence pointer where cheap to produce. No list edits.
- **5-7 Report hygiene.** Verify every NIGHT_01 manifest recomputes;
  verify no stray raw captures anywhere under live-runs; confirm the
  elevated relay is closed at run end; final end-of-run packet per
  Tier 0.
