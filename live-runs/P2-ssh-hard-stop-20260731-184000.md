# P2 SSH server observation — negative-evidence HARD STOP

## Outcome

Dedicated key authentication to `wowvmangos@192.168.0.2` was established and
confirmed with password authentication disabled. The key fingerprint and
setup shape are in `SETUP.md`; no password or private-key bytes appear in any
artifact. Password use ceased after the public-key installation, and rotation
is recommended.

The running server is deployed revision
`d7779aee9d43113e78c078b54daef89946be0b1a`. `mangosd` runs from
`/home/wowvmangos/vmangos/run/bin` in detached screen session `mangosd`, with
config at `/home/wowvmangos/vmangos/run/etc/mangosd.conf`. These paths, process
arguments, config keys, and source files were read only. No database query or
write occurred.

## Bounded capture

The accepted capture began from console/file level `2/2` and combat filter
OFF. It used the deployed parser's named-filter syntax, set console level 3 and
combat ON, ran one explicit `TEST`-character protocol, then restored and
re-queried `2/2` plus combat OFF. No persistent config edit or restart occurred.

The protocol completed with zero runner failures. Immediately before the real
send, the client recorded player GUID `0x0000000000000001`, server-confirmed
GM-OFF, target `0xF13000000604A288` present/visible/alive at 100/100, flags zero,
faction 25, and distance 0 yd. It then emitted one `CMSG_ATTACKSWING`.

The later attack-start/stop rows belong to a foreign creature attacker
`0xF13000066A01384C`, not player GUID 1. They do not satisfy player-GUID attack
acceptance.

## Required negative result

```text
PREDICTED: console debug yields a receive, dispatch, or attack-handler line for TEST
ACTUAL:    CMSG_ATTACKSWING=0; ATTACKSWING=0; HandleAttackSwing=0;
           received opcode=0; opcode 0x0141=0; opcode 321=0
VERBATIM:  At console log level 3 with LogFilter_Combat ON, the complete bounded
           mangosd console window contains no CMSG_ATTACKSWING receive, dispatch,
           opcode, or HandleAttackSwing admission line for TEST.
RESULT:    negative evidence; P2 HARD STOP; P3/P4 NOT STARTED
```

The deployed source explains the limit of the authorized observation. Normal
logged-in dispatch executes the handler without an admission log
(`WorldSession.cpp:520-588`), and the handler itself has no entry log
(`CombatHandler.cpp:32-62`). `Unit::Attack` also contains unlogged false returns
(`Unit.cpp:4721-4780`). Therefore this capture cannot distinguish pre-handler
loss from normal dispatch followed by an unlogged silent predicate. Naming a
root cause now would be invention. Deeper server instrumentation or debugging
requires a new order; no server code, DB, persistent config, client combat
behavior, error display, or F3-F6 work changed.

## Deviations closed

- The first SSH verifier failed because the sandbox-owned private key was not
  readable by the host network process. Ownership was transferred to Nico's
  Windows account and restricted to that account, SYSTEM, and Administrators;
  key-only authentication then passed.
- An initial console attempt used the previously recorded numeric filter form
  and received `Incorrect syntax`. Source inspection showed that this build
  requires the literal name `combat`. That attempt was discarded; the accepted
  run proves `combat = on` before the protocol and `combat = off` afterward.
- The first client launch was refused locally because the config leaves
  character selection blank. No connection occurred. The accepted run supplied
  `--character TEST`, preserving the dedicated-character safety law.

## Evidence

Run directory: `live-runs/P2-ssh-20260731-183142/`

- full server console: `server-console-20260731-183142.txt`
- exact searches: `capture-search-20260731-183142.txt`
- deployed source citations: `server-source-citations-20260731-183142.txt`
- runner/verdict, combat, movement, and full wire traces are colocated
- stage manifest: `live-runs/manifests/P2-ssh-20260731-184000.sha256`

## Gates

- Debug build: PASS, 0 warnings / 0 errors
- combat wire foundation: PASS
- portrait camera: PASS, specimens 10534; controls 1224 / 1289 / 56
- movement audit check: PASS

The first attempt to execute the independent checks concurrently collided in a
shared generated Release file. All checks were rerun sequentially and passed;
the collision changed no source or evidence.
