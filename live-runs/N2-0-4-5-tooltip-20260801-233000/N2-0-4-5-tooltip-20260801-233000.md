# NIGHT_02 4-5 — GameTooltip

Status: CLOSED-FINDING. Full-reference coverage is 1/1. Geometry passes; the
exact CSV retains three mechanical deltas for anchor metadata, backdrop-path
evidence, and strata/layer.

This increment adds a deterministic 220x80 real-draw tooltip specimen using
the same `WowSkin.Tooltip` nine-slice used by the client. It is armed only by
the parity protocol and therefore does not replace or fabricate runtime art.

Reference: `interface.MPQ:Interface\FrameXML\GameTooltip.xml`. Original art:
`interface.MPQ:Interface\Tooltips\UI-Tooltip-Background.blp` and
`interface.MPQ:Interface\Tooltips\UI-Tooltip-Border.blp`, as enumerated in
`tooltip-reference.csv`. No art was fabricated, approximated, or downloaded.

The contact sheet is `tooltip-contact.png`; `tooltip-diff.csv` is the exact
mechanical worklist. Gates: solution build, combat-wire, interface-wire,
portrait-camera, and move-audit PASS.
