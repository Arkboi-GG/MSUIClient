# NIGHT_01C 2-4 — loot

Run time: 2026-08-01 11:55 local

Status: `CLOSED-PASS`

## Prediction / actual / result

```text
PREDICTED: instrument and live-prove solo corpse loot, item/money collection,
           empty-corpse state, and release
ACTUAL:    exact wire/state checks pass; the isolated GM-provisioned corpse run
           passes 36/36 and the empty-state runtime replay passes 7/7
RESULT:    accepted — item, money, purse delta, empty state, and release all pass
```

## Protocol and increment

Deployed read-only vmangos source was checked before editing:

- `Server/Packets/Loot.cpp:3-16`: `u8` autostore slot and full-guid loot/release bodies.
- `Handlers/LootHandler.cpp:42-234`: solo autostore validation, item removal,
  item push, and error paths; money handling begins at line 236.
- Build-5875 opcodes: CMSG 264/349/350/351 and SMSG 352/353/354/355/357/358.

The existing loot frame/state was instrumented before the live attempt. Every
send and response now emits copyable `interface` verdicts, including exact body,
source, wire slot, item receipt/removal, money clear, independent purse delta,
empty response, and release. Network sends now report success, the runner has
loot actions, and the byte gate covers opcodes, bodies, parsing, empty state, and
the one-shot auto-release edge.

The instrument exposed one ordering issue: the final-money auto-release could
clear the visible loot session before the purse update was checked. The narrow
fix evaluates the pending economy delta independently of window visibility.

## Autonomous provisioning and live acceptance

Before first use, `.damage <positive amount> [school]` was verified at deployed
`Commands/UnitCommands.cpp:2403-2469`; it calls the test player’s `DealDamage`.
The world database was queried read-only to select creature 3229, whose loot has
guaranteed item 4951 and 3–9 copper. To prevent unrelated guards taking the tap,
the runner teleported to an isolated, already-known vantage before spawning.

The live runner delivered `.gps`, enabled GM only for teleport/spawn/damage,
disabled GM before loot acceptance, and passed 36/36:

```text
response OPEN: money=5, items=1, wire slot=0
item RECEIVED: entry=4951 count=1
item REMOVED: slot=0
money CLEARED: amount=5
economy VERIFIED: 28 -> 33, expected=5
release SENT and RELEASED: exact corpse GUID body
```

The independent empty-response/state replay delivered `.gps` and passed 7/7:
`money=0;items=0;autoRelease=false`, followed by explicit release. The parser/
state byte gate separately proves that an empty-at-open corpse does not arm the
last-row auto-release edge.

Evidence:

- `live-runs/runner-20260801-115029.csv`
- `live-runs/verdicts-20260801-115029.txt`
- `live-runs/runner-20260801-115203.csv`
- `live-runs/verdicts-20260801-115203.txt`
- `live-runs/N1C-2-4-loot-ui-20260801-115300.png`

The screenshot shows the authored corpse loot frame with the independent money
and item rows against the live dead creature.

## Boundary gates

- Debug build: PASS, 0 warnings / 0 errors
- combat-wire-check: PASS
- interface-wire-check: PASS
- portrait-camera-check: PASS, 10,534 / 1,224 / 1,289 / 56
- move-audit-check: PASS

Manifest: `live-runs/manifests/N1C-2-4-20260801-115500.sha256`.
