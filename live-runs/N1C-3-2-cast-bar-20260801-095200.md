# NIGHT_01C 3-2 — cast bar and cast-time behavior

Status: `CLOSED-FINDING`

## Increment

- Added read-only `SpellCastTimes.dbc` and `SpellDuration.dbc` joins to the
  spell catalog, with DBC-derived `INSTANT`, `CAST_TIME`, and `CHANNEL`
  classification.
- Added the `cast-bar` verdict channel and per-run CSV. It records server and
  DBC durations, start/end timestamps, phase, cumulative pushback, cancel
  source, and the exact animation state read by the renderer.
- Server start/go/failure, channel start/update/stop, cast delay, Escape, and
  movement cancellation now report through the same instrument.
- The live runner now waits for a spawned target to re-enter the post-teleport
  entity store before selecting it; this closes the observed stale-GUID setup
  race without weakening the cast gate.

## GM syntax actually verified

- RA `revive Test` returned `Test revived (offline)`. The deployed RA console
  has no `character revive` subcommand and no help entry for `revive`; both
  refusals and the successful root command are preserved.
- The live leg again used response-confirmed `.modify rage 1000`,
  `.cooldown clear`, `.gm off`, `.gps`, and cleanup on the dedicated TEST
  character.

## Live result

- The first probe's server byte `0x13` is now named
  `SPELL_FAILED_CASTER_DEAD`; the mapping is the vanilla build-5875 enum, not
  the shifted later-client enum. The run's manual `F-SILENT-INTERACT` marker is
  therefore rejected as a runner classification error and is not promoted.
- After the verified offline revive, Slam rank 1 (`1464`) passed the mechanical
  gate GM-off with a selected hostile unit, rage `9980`, cost `150`, DBC cast
  time `1500ms`, and a flushed `CMSG_CAST_SPELL`.
- The server did not send `SMSG_SPELL_START` in the four-second window. Moving
  caused a client cancel send and the server then returned the named
  `SPELL_FAILED_INTERRUPTED` response. The cast-bar CSV contains
  `CANCEL_SEND/CAST_TIME/MOVEMENT_CAST` followed by
  `CAST_FAILED/CAST_TIME/INTERRUPTED`.
- Start-duration and pushback display cannot be promoted from this leg because
  no server start was admitted. The implemented server-driven paths remain
  covered by the boundary executable and are ready for a responding session.

## Boundary gates

- Debug build: PASS, 0 warnings / 0 errors on the final incremental build.
- combat-wire: PASS.
- portrait-camera: PASS, 10,534 / 1,224 / 1,289 / 56.
- move-audit-check: PASS.

## Primary evidence

- `live-runs/runner-20260801-095038.csv`
- `live-runs/verdicts-20260801-095038.txt`
- `live-runs/spell-sweep-20260801-095038.csv`
- `live-runs/cast-bar-20260801-095038.csv`
- `dumps/sockettrace-n1c-castbar-slam-rank1-v8-20260801-095042.csv`
- `live-runs/N1C-gm-revive-root-20260801-094500.txt`
