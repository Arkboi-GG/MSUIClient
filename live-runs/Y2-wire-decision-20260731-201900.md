# SPEC-23 Y2 - transit decision

## Actual versus predicted

```text
PREDICTED row 1: attack payload present + ACKed + SPEC-21 server silence
ACTUAL: payload byte-substring absent from the capture view; ACK coverage unmeasured
RESULT: row 1 NOT SELECTED; server pre-handler/unlogged predicate not proven

PREDICTED row 2: chat present + attack absent/unACKed
ACTUAL: neither chat nor attack payload byte-substring appears
RESULT: row 2 NOT SELECTED; client/LAN anomaly not proven

PREDICTED row 3: neither payload present
ACTUAL: neither payload present in 44 zero-loss NIC-component formatted packets
RESULT: CAPTURE_FILTER_DEFECT; no causal transit row selected

PREDICTED accepted target proof: GM off, alive, flags zero, distance zero
ACTUAL: GM off and alive, but unitFlags=0x00080000 and distance=9.533476 yd
RESULT: SCENARIO_PRECONDITION_DRIFT; independent non-promotion reason
```

## Frozen decision

`CAPTURE_FILTER_DEFECT` is the only SPEC-23 decision supported by the packet
view. The pktmon capture was healthy as an ETW session (zero drops and zero
events lost), but `--comp nics` on the Wi-Fi adapter recorded client TCP
sequence progression as ACK-only packets while omitting the client
payload-bearing records required for byte-substring matching. The exact 14-byte
sequence advance is only circumstantial metadata and is not promoted to an
attack frame or server ACK.

The deployed X3 server candidate table is therefore not entered:

- `Server/WorldSocket.cpp:98-183`;
- `Server/WorldSession.cpp:277-331,518-549,1250-1313`;
- `Server/Protocol/Opcodes.cpp:398-401`;
- `Handlers/CombatHandler.cpp:32-62`;
- `Objects/Unit.cpp:4721-4804`.

Those locations remain frozen candidates only. SPEC-22 option 3 is not
authorized because arrival and ACK were not measured.

The bounded run was not repeated. A future order, if Nico chooses, must change
the measurement path rather than reinterpret this result: capture all pktmon
components or use the authorized netsh fallback, and mechanically prevent or
reject target drift before the single attack. That is a new ruling, not a fix
attempt under SPEC-23.

No server code, DB, persistent config, combat behavior, error display, or F3-F6
change occurred. SPEC-21 P3/P4 remain queued.
