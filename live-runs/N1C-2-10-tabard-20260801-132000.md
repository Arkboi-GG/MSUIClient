# NIGHT_01C 2-10 — guild tabard

Status: `CLOSED-FINDING`.

## Implemented increment

- Added the build-5875 tabard family: `MSG_SAVE_GUILD_EMBLEM` (`0x01F1`),
  `SMSG_TABARDVENDOR_ACTIVATE` (`0x01F2`), the full vendor GUID plus five-u32
  save body, result decoding, bounded verdict rows, designer UI, and live-run
  commands.
- Added tabard-designer targeting with the observed vanilla flag `0x00000400`.
- Added exact MPQ-backed character rendering. An equipped guild tabard now
  composites background, border, and emblem layers for both `TU` and `TL`
  atlas regions from `Textures\GuildEmblems` after the base tabard paint.
- Added a reusable vendor `buy-entry` runner path and exact buy-body gate; the
  live acceptance self-supplied item 5976 because interaction activation is in
  the already-registered F-SILENT family.

## Protocol/source verification

- Official vMaNGOS `GuildHandler.cpp` `HandleSaveGuildEmblemOpcode` requires a
  `UNIT_NPC_FLAG_TABARDDESIGNER`, guild leader, and 10 gold, then consumes the
  money, sets the five emblem fields, returns the emblem result, and refreshes
  guild query state:
  <https://github.com/vmangos/core/blob/development/src/game/Handlers/GuildHandler.cpp>.
- Build-5875 opcode values were cross-checked against the vMaNGOS 1.12 opcode
  table and the repository's existing benilla/vMaNGOS provenance chain.
- Blizzard's shipped 1.12 `Interface\FrameXML\TabardFrame.lua/xml` and the
  MPQ `(listfile)` established the five controls, 10-gold presentation, and
  exact `Background_`, `Border_`, and `Emblem_` TU/TL texture naming. The local
  archive contains styles 00–99, colors 00–16, borders, and backgrounds 00–50.

## GM command verification and isolation

- Previously verified deployed syntax was reused exactly: `.gm on|off`,
  `.gps`, `.guild create Test "MSUITabard"`, `.guild delete "MSUITabard"`,
  `.modify money 200000`, `.additem 5976 1`, `.npc spawn add 5193`, and
  `.npc spawn delete` with the spawned creature selected.
- The live object store independently identified entry 5193 with
  `npcFlags=0x00000485` (gossip + vendor + tabard designer), proving both entry
  and flag. The isolated guild and spawned NPC were deleted at the end; GM was
  off for open, save, money-delta, equip, and render acceptance legs.

## Live result

- Final runner: `live-runs/runner-20260801-131845.csv`, 40/40 steps passed.
- Full verdict stream: `live-runs/verdicts-20260801-131845.txt`.
- Delivered `.gps` controls bracketed the live leg.
- `tabard open` flushed the exact GUID body but activation was silent after the
  bounded wait; this is the single `BLOCKED-BY:F-SILENT-INTERACT` row for the
  leg, not a new finding family.
- Direct GM-off save flushed
  `C5A20449140030F1 07000000 03000000 02000000 05000000 0B000000` and the
  server returned `MSG_SAVE_GUILD_EMBLEM` error `0`.
- Independent player-field snapshots measured coin `699904 -> 599904`, the
  exact 100,000-copper (10-gold) purchase cost.
- GM-off item 5976 equip completed into equipment slot 18. The renderer gate
  bound all six exact MPQ layers for design `7/3/2/5/11`.

## Visual evidence

- Designer plus paper-doll: `live-runs/N1C-2-10-tabard-designer-20260801-131845.png`.
- Unobscured character paper-doll: `live-runs/N1C-2-10-tabard-character-20260801-131845.png`.
- Mechanical binding is accepted; visual/art judgment is queued as Q19.

## Gates

- `dotnet build MSUIClient.sln -c Debug`: pass, 0 warnings / 0 errors.
- combat-wire: pass.
- interface-wire: pass, including exact tabard opcodes/body and six-layer
  renderer binding.
- portrait-camera: pass.
- move-audit: pass.
