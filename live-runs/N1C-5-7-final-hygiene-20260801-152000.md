# NIGHT_01 5-7 — Final hygiene — 2026-08-01 15:20 local

Status: `CLOSED-FINDING`

## Hygiene result

- Worktree raw/diagnostic cleanup: 484 verified untracked files existed only under `live-runs/`
  and `scenarios/`. Irreversible deletion was rejected by the safety reviewer, so they were moved
  recoverably to ignored `dumps/archived-night01-untracked-20260801-151000/`. The worktree became
  clean and no untracked raw capture remains under `live-runs/`.
- Capture relay: no `pktmon`, `netsh`, `gdb`, `ssh`, `plink`, or `tcpdump` helper survives, and no
  PowerShell-owned listening socket was found for the inspected processes. The earlier relay exit
  remains closed.
- Final gates: Debug build (one pre-existing CA2014 warning, zero errors), combat wire, portrait
  camera (10,534 specimens; 1,224 / 1,289 / 56), and move audit all pass. Interface wire also
  passes.

## Manifest recomputation finding

The corrected verifier handled both GNU `*path` and plain `path` checksum formats. Across 42
NIGHT manifests / 467 entries, 20 manifests recompute completely and 22 are stale; there are zero
missing and zero malformed entries. The stale group hashes mutable source files and append-only
ledgers that later item commits necessarily changed. Immutable evidence-only manifests from the
spell flagship and final world/housekeeping items recompute. Existing evidence was not overwritten.

This is a hygiene-design finding, not missing evidence: per-item manifests must seal immutable
artifact paths only, or split source-at-commit identity from evidence hashes. Q21 requests that law
choice. The audit CSV is the complete manifest-by-manifest result.

## Evidence

- `live-runs/N1C-5-7-manifest-audit-20260801-151000.csv`
- `live-runs/manifests/N1C-5-7-final-hygiene-20260801-152000.sha256`
