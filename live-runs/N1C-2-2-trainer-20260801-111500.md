# NIGHT_01C 2-2 — trainer

Status: `CLOSED-PASS`

## Protocol and increment

- Deployed VMaNGOS source was read-only inspected at
  `src/game/Handlers/NPCHandler.cpp:91-273` and
  `src/game/Server/Packets/Npc.cpp:14-23`. Build 5875 uses full-GUID
  `CMSG_TRAINER_LIST` (`0x01B0`), `SMSG_TRAINER_LIST` (`0x01B1`),
  full-GUID-plus-service-spell `CMSG_TRAINER_BUY_SPELL` (`0x01B2`), and
  success/failure opcodes `0x01B3`/`0x01B4`.
- The bounded parser implements the exact 38-byte trainer row, including state,
  cost, primary-profession flags, level/skill requirements, and three spell
  prerequisites. Excessive counts and trailing bytes are rejected.
- Added gated list/buy sends, named list/buy/spellbook-delta verdicts, a usable
  trainer pane, and deterministic live-run commands. The UI exposes greeting,
  known/unavailable/available states, price, and requirements.
- The live-run wrapper still requires a configuration bound to character TEST,
  but now permits selecting a roster character on that same dedicated account.

## GM syntax verified and provisioning

- `.help modify money` returned no help on this build, so the deployed command
  registration and handler were verified read-only at `Chat.cpp:581-587` and
  `CharacterCommands.cpp:4460`; actual syntax is `.modify money <signed delta>`
  against the selected player.
- With `select self`, `.modify money 100000` changed the roster warrior's money
  to 100000. The spawned trainer used entry 911. GM was disabled before every
  list and purchase leg, and delivered `.gps` responses bracketed the flows.

## Live acceptance

- TEST list probe decoded five exact rows with one available service and the
  server greeting. The synthetic three-state pane is preserved in
  `N1C-2-2-trainer-ui-20260801-111000.png` for visual review.
- A fresh `Nbwarhuman` run passed 28/28 steps. The server returned five rows,
  exposed service spell 6674 for 10 copper, accepted the purchase, emitted
  `SMSG_LEARNED_SPELL` for spell 6673, and the client recorded
  `spellbook-delta=ADDED` with the known-spell count advancing to 41.
- Logical request bodies and parser bounds pass the interface-wire checker.
  Purchase success is not inferred from money or UI state: it is tied to the
  server success opcode and the independent spellbook delta.

## Evidence

- List runner/verdict: `runner-20260801-105815.csv`,
  `verdicts-20260801-105815.txt`.
- Learn runner/verdict: `runner-20260801-110553.csv`,
  `verdicts-20260801-110553.txt`.
- GM syntax transcript: `N1C-gm-money-help-20260801-110000.txt`.
- Visual: `N1C-2-2-trainer-ui-20260801-111000.png`.
- Manifest: `manifests/N1C-2-2-20260801-111500.sha256`.

## Boundary gates

- Debug build: PASS, 0 warnings / 0 errors.
- combat-wire: PASS.
- interface-wire: PASS, gossip + vendor + trainer bodies/bounds.
- portrait-camera: PASS, 10,534 / 1,224 / 1,289 / 56.
- move-audit-check: PASS.
