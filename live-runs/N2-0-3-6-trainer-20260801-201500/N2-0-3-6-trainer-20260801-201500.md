# NIGHT_02 3-6 — trainer

Status: CLOSED-FINDING. Full-reference coverage is 5/111 with 106 NOT-DRAWN; `trainer-diff.csv` is the exact worklist.

The generic trainer list now uses the original 384x512 trainer shell. Verified GM syntax provisioned entry 911 on the TEST account; GM invisibility was disabled, the authoritative trainer service list was requested and captured, then the temporary trainer was deleted. `trainer-contact.png` is the original-art reference/actual sheet.

Reference: `interface.MPQ:Interface\FrameXML\ClassTrainerFrame.xml`, explicitly selected because `patch.MPQ` contains a zero-byte shadow entry. No art was fabricated or downloaded.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
