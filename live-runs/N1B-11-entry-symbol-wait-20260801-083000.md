# NIGHT_01B item 1-1 — amended entry-symbol W1 wait packet

Run: 2026-08-01 07:48–08:23 local

Status: `SHELVED-BLOCKED` under NIGHT_01B amendment 7's item-1-1-only
interactive-wait rule. The remaining external input is a fresh ephemeral sudo
password entry; Nico explicitly ended babysitting after the completed W1F attach.
The autonomous client build queue continues.

## Actual versus predicted

```text
PREDICTED: dry attach resolves dispatch entry symbols and Unit::Attack return;
           one Z0-gated fresh-target packet runs concurrently with dprintf and
           bounded pktmon, then yields an amended W2 causal row
ACTUAL: dry attach PASS; all nine entry/return dprintf sites resolved and armed;
        three relay-control attempts were rejected before evidence promotion;
        W1E proved the scenario but ran it before trace/capture because of a local
        readiness-loop defect; W1F corrected concurrency but its fresh spawn was
        not observed, so the Z0 gate refused and no attack packet was constructed
RESULT: NO CAUSAL VERDICT; report=act preserved
```

## Attempt reconciliation

- W1B dry: sudo attach/detach PASS, symbols and `Unit::Attack+0x1f0` return
  resolved; first live helper stopped before capture on an expected pktmon
  pre-clean message.
- W1C: entries armed and detached, but no scenario was launched; control only.
- W1D: the client attempted login while attached, failed before protocol start,
  and was terminated by the authorized cleanup helper; control only.
- W1E: client protocol PASS 34/34, but the readiness loop did not exit and armed
  GDB/pktmon after the scenario; pktmon correctly recorded zero packets during
  the late window. This is an orchestration defect, not server evidence.
- W1F: readiness ordering fixed; GDB and pktmon were concurrent with the client,
  but `select spawn:1` failed, `AttackPreconditionGatePass` failed, and
  `attack start gate=REFUSED`. Socket trace contains no CMSG_ATTACKSWING. The
  880-packet raw capture was hashed and deleted without being promoted.

The W1F GDB transcript therefore has no attack-chain entry hit by construction.
It must not be interpreted as the amended W2 “no site fires” row because the
required packet does not exist.

## Hygiene and health

- Every raw ETL/PCAPNG/text capture from W1B–W1F was hashed then deleted.
- Every successful sudo use ended with `sudo -K`; no password is present in any
  artifact, file, environment, or command.
- `ptrace_scope=1` throughout; no server code/config/DB/sysctl/package/restart.
- Post-detach RA `server info` PASS (PID healthy, 512 players online) and a fresh
  TEST `.gps` round-trip PASS 2/2.
- The incidental `vantages.json` rewrite was restored.

Primary evidence roots are `live-runs/W1b-entry-symbol-20260801-074829/`
through `live-runs/W1f-entry-symbol-20260801-082000/`.

## Four-gate boundary

- Debug build: PASS, 0 warnings, 0 errors.
- combat-wire-check: PASS (its established CA2014 diagnostic only).
- portrait-camera-check: PASS, 10,534 specimens; controls 1,224 / 1,289 / 56.
- move-audit-check: PASS.
