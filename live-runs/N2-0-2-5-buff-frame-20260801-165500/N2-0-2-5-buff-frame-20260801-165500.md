# NIGHT_02 2-5 — buff/debuff frame

Status: `CLOSED-FINDING`

## Increment and real output

- Replaced the previous 12-column, 34px-pitch top-right aura row with the
  build-5875 `BuffFrame` anchor and 8-column, 35px-pitch wrap.
- Separated the 16 helpful and 8 harmful slots so harmful auras begin on the
  vanilla third row.
- Replaced the fabricated harmful border with the shipped debuff overlay and
  its exact FrameXML crop.
- Added actual-draw-path capture for the frame, first button, icon, and duration
  font; a TEST-only Arcane Intellect aura exercised the populated path.

## Mechanical finding

The populated representative cohort passes all 28 geometry, anchor,
texture-path, font, color, layer, and presence verdicts. The full 105-row
template/conditional inventory retains 101 presence deltas, so the family is
closed as a measured finding rather than a whole-family pass.

## Self-provision verification

VMaNGOS `ChatHandler::HandleNameAuraCommand` parses spell, player name, then
optional duration, and `HandleUnAuraCommand` removes the selected-unit spell
(`src/game/Commands/UnitCommands.cpp`, local indexed source). The protocol used
`.nameaura 1459 Test 3600`, observed `SMSG_UPDATE_OBJECT` APPLY for Arcane
Intellect, captured the real UI, then used `.unaura 1459` and observed REMOVE.

## Provenance

- `patch.MPQ:Interface\FrameXML\BuffFrame.xml`
- `interface.MPQ:Interface\Buttons\UI-Debuff-Overlays.blp`
- dynamic spell icon resolved from shipped DBC/MPQ data
- `fonts.MPQ:Fonts\FRIZQT__.TTF`

Build and live protocol: PASS.
