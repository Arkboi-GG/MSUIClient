# NIGHT_02 3-8 — mail

Status: CLOSED-FINDING. Full-reference coverage is 6/76 with 70 NOT-DRAWN; `mail-diff.csv` is the exact worklist.

The generic mailbox now uses the original 384x512 mail shell. The TEST account moved to the Stormwind mailbox and the real GET_MAIL_LIST wire was accepted. To guarantee a populated draw without direct database writes, the existing build-5875-shaped fixture was then invoked and remains explicitly labelled `REPLAYED` in the verdict evidence. `mail-contact.png` is the original-art reference/actual sheet.

Reference: `interface.MPQ:Interface\FrameXML\MailFrame.xml`. Art: `interface.MPQ:Interface\MailFrame\Mail-Icon.blp`, `interface.MPQ:Interface\ItemTextFrame\UI-ItemText-TopLeft.blp`, `-BotLeft.blp`, `interface.MPQ:Interface\Spellbook\UI-SpellbookPanel-TopRight.blp`, and `-BotRight.blp`. No art was fabricated or downloaded.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
