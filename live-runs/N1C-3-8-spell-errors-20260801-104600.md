# NIGHT_01C 3-8 — spell error display

Status: `CLOSED-PASS`

## Increment

- Added a `spell-error` verdict/CSV channel from the exact red center-text feed,
  recording stable reason enum, copyable display text, displayed/hidden state,
  spell identity, and source.
- Routed server `SMSG_CAST_RESULT` failures through named reason-to-text display
  before the cast bar enters its failed state. `DONT_REPORT` remains hidden and
  unknown bytes use a stable named enum plus generic visible fallback.
- Routed all local cast refusals through the same display/verdict path, including
  cooldown, in-progress, mounted, target, range, and resource gates.
- Added golden checks for line of sight, power-specific resource text, range,
  hidden `DONT_REPORT`, and unknown-byte fallback.

## Mechanical result

- The final protocol passed 42/42. Replayed server result packets through the
  production handler produced copyable red-line text for line of sight, mana,
  range, and unknown `0xFE`; `SPELL_FAILED_DONT_REPORT` emitted a verdict with
  `displayed=False` and no text.
- With GM off, a real Fireball attempt at zero mana was refused locally as
  `LOCAL_NO_POWER` and displayed `Not enough mana`. After autonomous mana and
  cooldown provisioning plus a same-map teleport, a second real attempt was
  refused as `LOCAL_OutOfRange` and displayed `Out of range.` Both legs had
  delivered `.gps` controls and Z0 spell/target/resource rows.
- Earlier accepted live rows remain complementary server-origin evidence:
  item 3-2 returned `SPELL_FAILED_INTERRUPTED`, while channel probing returned
  named unknown `SPELL_FAILED_0x7C`. No server failure is collapsed to the old
  generic `FAILED` label anymore.

## Primary evidence

- `live-runs/runner-20260801-104340.csv`
- `live-runs/spell-error-20260801-104340.csv`
- `live-runs/spell-sweep-20260801-104340.csv`
- `scenarios/spells/spell-errors-live.txt`

## Boundary gates

- Debug build: PASS, 1 pre-existing CA2014 warning / 0 errors.
- combat-wire: PASS, including reason-to-display-text goldens.
- portrait-camera: PASS, 10,534 / 1,224 / 1,289 / 56.
- move-audit-check: PASS.
