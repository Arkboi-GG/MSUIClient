# NIGHT_02 3-5 — merchant

Status: CLOSED-FINDING. Full-reference coverage is 5/124 with 119 NOT-DRAWN; `merchant-diff.csv` is the exact worklist.

The generic vendor window was replaced by the shipped 384x512 merchant shell. The TEST account teleported to Stormwind, measured the nearest vendor, moved in range, requested the authoritative inventory, and captured a populated real draw path. `merchant-contact.png` is the original-art reference/actual sheet.

Reference: `patch.MPQ:Interface\FrameXML\MerchantFrame.xml`; no art was fabricated or downloaded.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
