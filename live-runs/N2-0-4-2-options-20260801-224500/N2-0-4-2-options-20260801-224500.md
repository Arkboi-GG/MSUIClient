# NIGHT_02 4-2 — options

Status: CLOSED-FINDING. Full-reference coverage is 2/180 with 178 NOT-DRAWN; `options-diff.csv` is the exact worklist.

The capture hook now opens the real video-options page rather than aliasing it to the game menu, and records the actual 540x450 responsive host plus shipped header plaque. The substantial vanilla control hierarchy remains honestly NOT-DRAWN; existing functional settings controls are visible in the contact but are not counted until individually draw-instrumented.

Reference: `patch.MPQ:Interface\FrameXML\OptionsFrame.xml`. Art paths and suppliers are enumerated in `options-reference.csv`; the host uses the shipped DialogFrame background/border/header. No art was fabricated or downloaded.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
