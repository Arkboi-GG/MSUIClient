# NIGHT_02 3-3 — talent frame

Status: CLOSED-FINDING. Full-reference coverage is 9/31 with 22 NOT-DRAWN; `talent-frame-diff.csv` is the exact worklist.

The absent vanilla presentation was built: the real window now uses the original 384x512 shell plus the selected class talent-tree background split across the four shipped regions. Paths and geometry are shared by draw and evidence. The valid reference was extracted explicitly from `interface.MPQ:Interface\FrameXML\TalentFrame.xml` because `patch.MPQ` contains a shipped zero-byte shadow entry.

`talent-frame-contact.png` is the original-art reference/actual sheet. No art was invented or downloaded.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
