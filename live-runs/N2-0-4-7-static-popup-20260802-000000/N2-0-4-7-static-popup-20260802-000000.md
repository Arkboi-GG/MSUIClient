# NIGHT_02 4-7 — cursor + dialog/popup

Status: CLOSED-FINDING. Full-reference StaticPopup1 coverage is 5/17 with 12
NOT-DRAWN; `static-popup-diff.csv` is the exact mechanical worklist.

This increment loads and draws the original dialog-button states plus alert
icon alongside the existing shipped dialog nine-slice. It adds a deterministic
two-button confirmation specimen. The first compile-failure attempt is retained
in the run history by the build output; the final non-overwriting live capture
is successful. The existing `DrawCarriedItem` path already attaches the real
item icon to the pointer for authoritative picked-up inventory state.

Reference: `interface.MPQ:Interface\FrameXML\StaticPopup.xml`. Assets are
enumerated in `static-popup-reference.csv`, including
`interface.MPQ:Interface\DialogFrame\UI-DialogBox-Background.blp`, border,
alert icon, and `Interface\Buttons\UI-DialogBox-Button-*.blp`. No art was
fabricated, approximated, or downloaded.

The contact sheet is `static-popup-contact.png`. Gates: solution build,
combat-wire, interface-wire, portrait-camera, and move-audit PASS.
