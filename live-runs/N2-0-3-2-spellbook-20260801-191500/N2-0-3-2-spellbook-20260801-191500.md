# NIGHT_02 3-2 — spellbook

Status: CLOSED-FINDING. Full-reference coverage is 6/160 with 154 NOT-DRAWN. `spellbook-diff.csv` is the exact full-reference worklist; coverage precedes verdicts.

The real draw path records the 384x512 host and five shipped background/icon regions from the same path/geometry variables consumed by drawing. `spellbook-contact.png` pairs the original FrameXML/MPQ render with the populated TEST-account spellbook.

Reference: `patch.MPQ:Interface\FrameXML\SpellBookFrame.xml`; all asset suppliers are recorded per CSV row; no fabricated/downloaded art.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
