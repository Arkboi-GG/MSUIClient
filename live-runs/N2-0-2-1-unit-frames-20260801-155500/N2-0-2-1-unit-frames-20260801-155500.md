# NIGHT_02 2-1 — player / target unit frames

Status: `CLOSED-FINDING`

## Increment and real output

- Added report=act live capture for the PlayerFrame and TargetFrame root,
  background, and portrait geometry.
- Corrected the capture's logical-vs-physical UI scale; all recorded geometry,
  anchors, layers, and strata now match FrameXML.
- Corrected target aura geometry to build-5875's separate 17px debuff grid
  (5,68; 20px pitch) and 21px buff row (5,87; 24px pitch).
- Two TEST-character protocols passed. The target leg selected a real nearby
  creature before capturing the presented framebuffer.

## Mechanical finding

The measured root/background/portrait rows pass. The default-visible extraction
still reports 9 player-frame and 28 target-frame presence deltas. Most are
conditional/template-owned rows (rest/attack icons, target aura button pool,
dead/skull state) that the current capture does not yet enumerate; the frame
texture, label, level, and target name-background rows are real draws that still
need report=act instrumentation. This is not a parity PASS.

Full FrameXML tables and representative-state tables are both preserved so the
finding cannot be hidden by changing the cohort. Contact sheets are queued for
perceptual review only.

## Provenance

- `patch.MPQ:Interface\FrameXML\PlayerFrame.xml`
- `patch.MPQ:Interface\FrameXML\TargetFrame.xml`
- `interface.MPQ:Interface\TargetingFrame\UI-TargetingFrame.blp`
- `interface.MPQ:Interface\TargetingFrame\UI-TargetingFrame-LevelBackground.blp`
- `interface.MPQ:Interface\TargetingFrame\UI-StatusBar.blp`
- `fonts.MPQ:Fonts\FRIZQT__.TTF`

Build: PASS with the standing CA2014 warning only. Live protocols: PASS.
