# SPEC-23 Y0 - elevated capture relay preflight

## Actual versus predicted

```text
PREDICTED direct agent elevation: net session PASS, High Mandatory Level present
ACTUAL direct agent process: ELEVATION_ABSENT on three bounded attempts
RESULT: stopped each attempt without UAC retry loops

PREDICTED Nico-authorized workaround: one-shot elevated relay limited to capture control
ACTUAL relay: net session exit=0; S-1-16-12288 present
RESULT: ELEVATION PASS at the capture-control boundary

PREDICTED pktmon: status succeeds where SPEC-22 X2 saw driver access denied
ACTUAL: pktmon status exit=0 and reports not running
RESULT: PKTMON DRIVER ACCESS PASS

PREDICTED bounded filter: TCP 192.168.0.2:8085 only
ACTUAL: exactly one filter, SPEC23-Y1 / TCP / 192.168.0.2 / 8085
RESULT: FILTER PREFLIGHT PASS

PREDICTED repository: clean at 2c71edb or descendant except supplied SPEC-23
ACTUAL: HEAD 2c71edb; only SPEC_TOOLKIT_23_ATTACK_WIRE_CAPTURE.md was supplied untracked
RESULT: REPOSITORY PREFLIGHT PASS; order document preserved in the Y0 commit

PREDICTED live reachability: dedicated key SSH and read-only RA server info succeed
ACTUAL: KEY_AUTH_OK; RA authenticated and returned core/server information
RESULT: CONNECTIVITY PASS
```

## Authorized procedural deviation

Nico explicitly authorized a temporary elevated capture relay after the desktop
task's command process remained at medium integrity despite the app being launched
as Administrator. The relay is hardcoded to this repository, run directory,
`192.168.0.2:8085`, and the following operations only: elevation/status proof,
pktmon filter/start/stop/conversion, and deletion of this order's ETL/pcapng.
It provides no arbitrary command channel and makes no service, firewall, install,
server, database, or persistent configuration changes.

## Four gates

- Debug build: PASS; established CA2014 warning only, zero errors.
- combat-wire: PASS.
- portrait-camera: PASS; 10,534 specimens and 1,224 / 1,289 / 56 controls.
- move-audit: PASS.

The first parallel launch of the three independent gate executables contended on
the shared Release build output. Each gate was immediately rerun sequentially and
passed; this was a verification-process collision, not a source or product defect.

Y1 is authorized. SPEC-21 P3/P4 remain queued.
