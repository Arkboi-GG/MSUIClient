# NIGHT_02 4-1 — game menu

Status: CLOSED-FINDING. Full-reference coverage is 10/43 with 33 NOT-DRAWN; `game-menu-diff.csv` is the exact worklist.

The pre-pilot game-menu capture used call-site copies of reference metadata and was therefore instrument debt. This increment replaces the root, shipped header plaque, and eight actual button rows with draw-variable-derived evidence and captures the real modal draw path.

Reference: `patch.MPQ:Interface\FrameXML\GameMenuFrame.xml`. Art: `patch.MPQ:Interface\DialogFrame\UI-DialogBox-{Background,Border,Header}.blp` and `interface.MPQ:Interface\Buttons\UI-Panel-Button-Up.blp` as resolved in the CSV. No art was fabricated or downloaded.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
