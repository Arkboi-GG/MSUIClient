# NIGHT_02 3-11 — guild

Status: CLOSED-FINDING. Full-reference coverage is 1/63 with 62 NOT-DRAWN; `guild-diff.csv` is the exact worklist.

`GuildFrame` is an embedded, unsized child of `FriendsFrame`; the committed rooted CSV records the 384x512 host geometry without mutating the original extraction. The TEST account created a temporary guild and obtained a real roster before invoking the explicitly replay-labelled expanded state, then deleted the guild. The generic window now uses the shipped FriendsFrame shell.

Reference: `interface.MPQ:Interface\FrameXML\FriendsFrame.xml`. Host art: the four `interface.MPQ:Interface\FriendsFrame\UI-FriendsFrame-{TopLeft,TopRight,BotLeft,BotRight}.blp` regions. No art was fabricated or downloaded.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
