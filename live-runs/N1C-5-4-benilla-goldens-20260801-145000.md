# NIGHT_01 5-4 — Benilla golden traces Stage B — 2026-08-01 14:50 local

Status: `SHELVED-RULING` (existing Q11)

## Committed increment and actual attempt

Added `move-audit --golden-diff <msui> <benilla> <output>` so every measured movement metric now
has copyable `msui`, `benilla`, and `delta` columns. A same-trace plumbing control produced 16/16
zero deltas, proving the comparison path before attempting to source a golden.

The recorded checkout `C:\Users\nico\Desktop\benilla-main` exists. The recorded launcher
`run-benilla.ps1` does not exist in that checkout. The fallback build command was actually
attempted from that working directory; `cargo` is not installed or available on PATH, and no
per-user Rust toolchain executable was present. Read-only source assessment still finds direct
Bevy keyboard input and no scripted movement source or per-tick movement CSV dumper. Therefore no
Benilla trace was fabricated or mislabeled.

Q11 remains the binding ruling under Amendment 1B: supply the personalized runnable invocation
and toolchain, or accept the I14-authorized downgrade to an eyeball-only reference. Recommendation:
accept the downgrade; analytic vanilla bands remain the primary law and the golden-diff consumer
is ready if a real trace is later supplied.

## Evidence

- `live-runs/N1C-5-4-golden-diff-plumbing-20260801-145000.csv`
- `SETUP.md` — recorded checkout/launcher assessment
- `tools/move-audit/Program.cs` — additive golden-diff consumer

