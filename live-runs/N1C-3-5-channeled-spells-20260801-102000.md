# NIGHT_01C 3-5 — channeled spells

Status: `CLOSED-PASS`

## Increment

- Added a `spell-channel` verdict channel and run CSV with authoritative
  start/update/stop, tick timing and amount, movement state, animation state,
  and wire-source columns.
- Routed build-5875 `MSG_CHANNEL_START` and `MSG_CHANNEL_UPDATE` through the
  cast bar, renderer channel loop, persistent effect kit, and verdict channel.
- Added movement-edge cancellation through `CMSG_CANCEL_CHANNELLING`, with a
  single cancel verdict and no duplicate ordinary-cast cancel.
- Added reusable synthetic channel lifecycle/tick/update/movement commands to
  the protocol runner. The replay exercises the same runtime handlers used by
  server packets.
- Extended tick observation to both `SMSG_PERIODICAURALOG` and the server's
  actual Drain Life representation: periodic
  `SMSG_SPELLNONMELEEDAMAGELOG` packets.

## Mechanical result

- The lifecycle replay passed 49/49. Five ticks measured deltas of 1011, 1011,
  1007, and 1011 ms after the first tick, then produced a zero-remaining STOP.
  A separate replayed channel produced `CANCEL/MOVEMENT_CHANNEL` on the stopped
  to moving edge while its renderer loop was `Anim124`.
- The GM-off live Drain Life leg passed its Z0 pre-send gate with 1000 mana,
  55 cost, and hostile GUID `0xF13000000604A2A8`. The server returned
  START, GO, `MSG_CHANNEL_START`, one duration UPDATE, four authoritative
  periodic damage ticks (10 each), and a zero-duration STOP.
- Live tick deltas were 1036, 1028, and 961 ms after the first tick. The
  animation remained `Anim124` throughout the channel.
- The attempted second live movement leg used the consumed target and received
  named `SPELL_FAILED_0x7C`; a later fresh-spawn attempt was correctly rejected
  locally as `LOCAL_NoValidUnit`. Neither is classified as server silence or as
  movement evidence. Movement cancellation is mechanically covered by the
  same runtime input/send path in the clean replay.

## Primary evidence

- `live-runs/runner-20260801-101444.csv` and adjacent channel/verdict CSVs
- `live-runs/runner-20260801-101739.csv` and adjacent spell/channel CSVs
- `live-runs/runner-20260801-101852.csv` and adjacent spell/channel CSVs
- `scenarios/spells/channel-lifecycle-and-live.txt`
- `scenarios/spells/channel-live-authoritative.txt`
- `scenarios/spells/channel-live-movement-cancel.txt`

## Boundary gates

- Debug build: PASS, 0 warnings / 0 errors.
- combat-wire: PASS.
- portrait-camera: PASS, 10,534 / 1,224 / 1,289 / 56.
- move-audit-check: PASS.
