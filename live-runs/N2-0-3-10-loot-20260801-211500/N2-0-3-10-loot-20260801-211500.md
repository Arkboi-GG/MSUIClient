# NIGHT_02 3-10 — loot

Status: CLOSED-FINDING. Full-reference coverage is 4/20 with 16 NOT-DRAWN; `loot-diff.csv` is the exact worklist.

The existing original-art loot draw was instrumented rather than replaced. The TEST account provisioned and killed entry 3229, requested the authoritative corpse loot, captured the populated real draw path, released loot, and deleted the temporary creature. `loot-contact.png` is the original-art reference/actual sheet.

Reference: `interface.MPQ:Interface\FrameXML\LootFrame.xml`. Art: `interface.MPQ:Interface\LootFrame\UI-LootPanel.blp`, `interface.MPQ:Interface\TargetingFrame\TargetDead.blp`, and the shipped button/icon paths in `loot-reference.csv`. No art was fabricated or downloaded.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
