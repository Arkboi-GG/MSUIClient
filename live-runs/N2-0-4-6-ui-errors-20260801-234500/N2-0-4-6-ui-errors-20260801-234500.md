# NIGHT_02 4-6 — error / UI-message text

Status: CLOSED-FINDING. Full-reference coverage is 2/2; geometry and the root
anchor pass. Five exact deltas remain across strata/layer, child anchor
metadata, font-object identity, and the dynamic red error color.

This increment adds a deterministic real-draw `UIErrorsFrame` specimen at the
vanilla 512x60 placement using the existing foreground UI path. The displayed
error uses the shipped FRIZQT face already loaded by the client.

Reference: `interface.MPQ:Interface\FrameXML\UIErrorsFrame.xml`. Font asset:
`fonts.MPQ:Fonts\FRIZQT__.TTF`, enumerated in `ui-errors-reference.csv`. No art
or typeface was fabricated, approximated, downloaded, or copied from outside
the shipped archive set.

The contact sheet is `ui-errors-contact.png`; `ui-errors-diff.csv` is the exact
mechanical worklist. Gates: solution build, combat-wire, interface-wire,
portrait-camera, and move-audit PASS.
