# NIGHT_02 4-4 — macro UI

Status: CLOSED-FINDING. Full-reference coverage is 6/158 with 152 NOT-DRAWN; `macro-diff.csv` is the exact worklist.

The repository had no macro frame and the game-menu route was disabled. This increment adds the shipped 384x512 shell, portrait icon, local six-slot grid, name/body editors, new/delete actions, and enables the route. A first preserved run truthfully records a compile failure from the initial editor overload; the non-overwriting final run is the successful real draw.

Reference: `interface.MPQ:Interface\FrameXML\MacroFrame.xml`. Art paths and suppliers are enumerated in `macro-reference.csv`, led by the PaperDoll top and MacroFrame bottom regions and shipped `MacroFrame-Icon.blp`. No art was fabricated or downloaded.

Gates: solution build, combat-wire, interface-wire, portrait-camera, and move-audit PASS.
