# NIGHT_01 4-5 — Innkeeper / hearth completion — 2026-08-01 14:20 local

Status: `CLOSED-FINDING`

## Increment

Added exact 1.12 binder activate, binder confirm, and bind-point update protocol; innkeeper
eligibility/range gates; bind confirmation and current-home UI; Hearthstone item discovery and
server spell 8690 use; server cast completion, bind update, teleport, and cooldown observations;
and unattended live/replay runner cells.

## Autonomous provisioning and live acceptance

The deployed command forms previously verified by source/help were reused: `.gm on|off`,
`.additem 6948 1`, `.npc spawn add 295`, `.npc spawn delete`, `.cooldown clear`, and `.gps`.
The add-item command reported that another Hearthstone could not be created because TEST already
owned item 6948; the runner found that existing server item at bag 255 slot 24 and used it. The
temporary innkeeper had the real `0x87` NPC flags and TEST was placed at zero measured range.

With GM OFF, the client flushed `CMSG_BINDER_ACTIVATE` with the exact eight-byte innkeeper GUID.
The bounded bind-confirm wait retained the single registered `F-SILENT-INTERACT` finding. With GM
OFF, item 6948 then produced real `SMSG_SPELL_START` and `SMSG_SPELL_GO` for spell 8690 across the
10-second server cast, followed by the server's home bind update at the deployed home location.
The exact runtime path also replayed a distinct 550-yard destination to prove teleport-distance
classification and the one-hour cooldown display. The protocol passed 26/26, bracketed by
delivered `.gps` controls.

## Evidence

- `live-runs/runner-20260801-140535.csv`
- `live-runs/verdicts-20260801-140535.txt`
- `live-runs/N1C-4-5-hearth-ui-20260801-140535.png`
- `scenarios/world/hearth-live.txt`

