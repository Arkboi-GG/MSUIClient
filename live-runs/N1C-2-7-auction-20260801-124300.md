# NIGHT_01C 2-7 — auction house

Run time: 2026-08-01 12:43 local

Status: `CLOSED-FINDING`

## Prediction / actual / result

```text
PREDICTED: build auction browse/create/bid/buyout/cancel/owner-list wire, state, UI, pagination, deposit, and mail interplay
ACTUAL:    exact client family and 39/39 acceptance pass; live create returns the server's restricted-account result
RESULT:    closed finding — the client increment is complete and the mutation refusal is explicitly server-authoritative
```

## Protocol and parser increment

Read-only access to the deployed checkout was attempted first and the established
SSH key was refused. No server mutation was attempted. The same official vMaNGOS
source family was then read directly:

- `Server/Packets/AuctionHouse.cpp`: exact incoming request body order.
- `Handlers/AuctionHouseHandler.cpp:41,230,412,569,708,731,752`: hello,
  create, bid, cancel, bidder-list, owner-list, and browse handlers.
- `AuctionHouse/AuctionHouseMgr.cpp:98-109,711-842`: deposit calculation,
  50-row pagination, and exact result-row serialization.
- `Opcodes_1_12_1.h`: build-5875 opcodes 597-607, 612-613, and 653.

The client now implements exact hello, browse/filter/page, owner/bidder lists,
create, bid/buyout, cancel, command-result, list-result, and notification wire;
bounded 64-byte row decoding; item-template discovery; a complete auction UI;
copyable verdicts; runner commands; and byte/body checks. The house-1 deposit
preview follows the official formula using its 15 percent deposit rate, item
sell price, stack count, and 720-minute periods, with a minimum of one copper.

## Live leg and finding

The runner provisioned item 159 and 100,000 copper, spawned auctioneer 8670,
mechanically selected its auctioneer NPC flag at distance zero, and disabled GM
before acceptance sends. Live hello opened house 1, browse decoded an empty
result, and owner-list decoded an empty result. The exact create body flushed,
then the server returned `AUCTION_ERR_RESTRICTED_ACCOUNT` (`error=2`). Official
handler source shows that a security-bearing account is rejected when GM trades
are disabled, even while GM mode itself is off. This is an explicit external
policy result, not silence and not a client transport failure.

The runtime replay then decoded two exact 64-byte rows, verified pagination
(`2 of 77`, next start 50), create/bid/cancel success results, the deposit
formula, bidder/owner/removed notifications, and the existing
`SMSG_RECEIVED_MAIL` refresh interplay. The scenario passed 39/39 checks. Both
`.gps` positive controls were delivered and the spawned auctioneer was removed.

Evidence:

- `live-runs/runner-20260801-124131.csv`
- `live-runs/verdicts-20260801-124131.txt`
- `live-runs/N1C-2-7-auction-ui-20260801-124200.png`

## Boundary gates

- Debug build: PASS, established CA2014 warning only, 0 errors
- combat-wire-check: PASS
- interface-wire-check: PASS
- portrait-camera-check: PASS, 10,534 / 1,224 / 1,289 / 56
- move-audit-check: PASS

Manifest: `live-runs/manifests/N1C-2-7-20260801-124300.sha256`.
